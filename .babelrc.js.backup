module.exports = (api) => {
  const isTest = api.env('test')
  
  if (isTest) {
    return {
      presets: [
        'next/babel',
        ['@babel/preset-env', { targets: { node: 'current' } }],
        '@babel/preset-react'
      ]
    }
  }
  
  // For development/production, return undefined to let Next.js use SWC
  return undefined
}